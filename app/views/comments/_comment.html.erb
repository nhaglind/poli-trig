
<%= div_for(comment) do %>
		<p><%= comment.body %></p>
		<footer class="blockquote-footer">
			<small>Submitted <strong><%= time_ago_in_words(comment.created_at) %> ago</strong> by <%= comment.user.name %></small></p>
    </footer>
<% end %>