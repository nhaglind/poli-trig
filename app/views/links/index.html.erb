<% @links.each do |link| %>
  <h3><%= link_to link.title, link %> <a class="btn btn-default btn-sm float-right" href="<%= link.url %>">Visit Link</a></h3>
  <p>Submitted <%= time_ago_in_words(link.created_at) %> by User ID: <%= link.user.id %></p>

    <div class="btn-group">
      <%= link_to like_link_path(link), method: :put, class: "btn btn-primary btn-sm" do %>
        <span class="glyphicon glyphicon-chevron-up"></span>
        Upvote
        <%= link.get_upvotes.size %>
      <% end %>
      <%= link_to dislike_link_path(link), method: :put, class: "btn btn-primary btn-sm" do %>
        <span class="glyphicon glyphicon-chevron-down">
        Downvote
        <%= link.get_downvotes.size %>
      <% end %>
    </div>

    <hr>
<% end %>